return {
    {
        "kassio/neoterm",
        -- lazy load on these commands
        cmd = {"T", "Tnew", "Texec", "Tls", "Topen"},
        init = function()
            -- neoterm config goes here
        end,
    }
}
